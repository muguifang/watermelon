<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.muguifang.mapper.myMapper.TMyCollectMapper" >
  <select id="getMyCollect" parameterType="java.lang.Integer" resultType="java.util.Map">
    SELECT
	tc.id,
	tm.musicName mName,
	tc.insertDate insertdate
    FROM
	t_collect tc
	LEFT JOIN t_music tm ON tc.music_id = tm.id
    where tc.user_id = #{_parameter}
    ORDER BY
	tc.insertDate DESC
  </select>
</mapper>